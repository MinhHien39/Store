services:
  db:
    profiles: ["local", "dev", "stg", "prd"]
    image: ${APP_NAME}-db-${ENV}
    container_name: ${APP_NAME}-db-${ENV}
    build:
      context: ./database
      dockerfile: Dockerfile
    environment:
      MYSQL_ROOT_PASSWORD: ${DATABASE_ROOT_PASSWORD}
      DATABASE_NAME: ${DATABASE_NAME}
      DATABASE_USER: ${DATABASE_USER}
      DATABASE_PASSWORD: ${DATABASE_PASSWORD}
      TZ: Asia/Ho_Chi_Minh
    ports:
      - ${DATABASE_PORT}:${DATABASE_PORT}
    volumes:
      - mysql-data:/var/lib/mysql
      - type: bind
        source: ${HOST_PROJECT_DIR:-.}/database/mysql.cnf
        target: /etc/mysql/conf.d/mysql.cnf
        read_only: true
        bind:
          create_host_path: false
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "${DATABASE_USER}", "-p${DATABASE_PASSWORD}"]
      interval: 10s
      timeout: 5s
      retries: 5
    mem_limit: 512m
    memswap_limit: 512m
    restart: unless-stopped

  redis:
    profiles: ["local", "dev", "stg", "prd"]
    image: ${APP_NAME}-redis-${ENV}
    container_name: ${APP_NAME}-redis-${ENV}
    build:
      context: ./redis
      dockerfile: Dockerfile
    environment:
      - REDIS_PASSWORD=${REDIS_PASSWORD}
      - TZ=Asia/Ho_Chi_Minh
    ports:
      - ${REDIS_PORT}:${REDIS_PORT}
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    mem_limit: 64m
    restart: unless-stopped

  backend:
    profiles: ["local", "dev", "stg", "prd"]
    image: ${APP_NAME}-backend-${ENV}
    container_name: ${APP_NAME}-backend-${ENV}
    build:
      context: ./backend
      dockerfile: Dockerfile
      args:
        ENV: ${ENV}
    environment:
      - TZ=Asia/Ho_Chi_Minh
    ports:
      - ${BACKEND_PORT}:${BACKEND_PORT}
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - type: bind
        source: ${HOST_PROJECT_DIR:-.}/environment
        target: /environment
      - type: bind
        source: ${HOST_PROJECT_DIR:-.}/backend
        target: /backend
      - type: bind
        source: ${HOME}/.aws/${APP_NAME}
        target: /root/.aws
      - backend-uploads:/backend/uploads
      - backend-logs:/backend/logs
    mem_limit: 1024m
    memswap_limit: 1024m
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8000/api/v1/get-healthy >/dev/null || exit 1"]
      interval: 60s
      timeout: 5s
      retries: 5
      start_period: 30s

  frontend:
    profiles: ["local", "dev", "stg", "prd"]
    image: ${APP_NAME}-frontend-${ENV}
    container_name: ${APP_NAME}-frontend-${ENV}
    build:
      context: ./frontend
      dockerfile: Dockerfile
    environment:
      - TZ=Asia/Ho_Chi_Minh
      - ENV=${ENV}
      - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL}
      - BACKEND_INTERNAL_URL=http://backend:8000
    ports:
      - ${NEXT_PUBLIC_PORT}:3000
    volumes:
      - type: bind
        source: ${HOST_PROJECT_DIR:-.}/environment
        target: /environment
      - type: bind
        source: ${HOST_PROJECT_DIR:-.}/frontend
        target: /frontend
      - /frontend/node_modules
      - /frontend/.next
    restart: always

  nginx:
    profiles: ["dev", "stg", "prd"]
    image: ${APP_NAME}-nginx-${ENV}
    container_name: ${APP_NAME}-nginx-${ENV}
    build:
      context: .
      dockerfile: ./nginx/Dockerfile
      args:
        ENV: ${ENV}
    environment:
      - TZ=Asia/Ho_Chi_Minh
      - SERVER_NAME=${SERVER_NAME}
      - ENV=${ENV}
    ports:
      - "80:80"
    depends_on:
      - backend
      - frontend
    mem_limit: 64m
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "nginx", "-t"]
      interval: 30s
      timeout: 10s
      retries: 3
    volumes:
      - backend-uploads:/backend/uploads:ro
      - nginx-logs:/var/log/nginx

volumes:
  mysql-data:
  redis-data:
  backend-uploads:
  backend-logs:
  nginx-logs:
